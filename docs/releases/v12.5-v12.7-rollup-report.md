# V12.5-V12.7 Rollup Validation Report

Date: 2026-06-07
Scope: V12.5, V12.6, and V12.7 only.
State: all three slices implemented, validated, and committed separately.

This rollup intentionally stops at V12.7. No V12.8/V12.9 implementation, observability work, pricing provenance work, or production certification was started.

## 1. Slice Summary

### V12.5 - Inventory Stale-Count Guard

Commit:
- `0e75f32 fix: add v12 inventory stale-write protection`

Primary changes:
- added SQL-side stale stock-count protection
- blocked stale apply after waste/manual adjustment
- preserved idempotent same-line apply behavior
- added `scripts/verify-inventory-integrity.mjs`
- updated ops-capture safe error handling

Validation report:
- `docs/releases/v12.5-validation-report.md`

### V12.6 - Checklist / Compliance Evidence Integrity

Commit:
- `a1abf51 fix: harden v12 checklist evidence integrity`

Primary changes:
- added versioned checklist definitions and definition steps
- bound sessions to server-known definitions
- validated step keys and evidence payloads in SQL
- blocked empty/incomplete checklist completion before audit emission
- expanded receipt metadata while preserving legacy reads
- added `scripts/verify-checklist-integrity.mjs`

Validation report:
- `docs/releases/v12.6-validation-report.md`

### V12.7 - Operational Truth Layer

Commit:
- `8cc195a fix: add v12 operational truth states`

Primary changes:
- added typed `DataResult` states
- added runtime truth helpers and explicit demo fallback policy
- required canonical storefront branch configuration in production
- added result-returning catalog/pickup/order loaders
- removed silent production demo fallback from covered storefront/admin paths
- added shared `OperationalSnapshotV1` for `/admin` and `/admin/today`
- added owner/public truth-state banners
- added `scripts/verify-operational-truth.mjs`

Validation report:
- `docs/releases/v12.7-validation-report.md`

## 2. Final Combined Gate

Final gate was run after the V12.7 commit from commit:
- `8cc195a`

| Command | Result |
|---|---|
| `pnpm typecheck` via `.\pnpm.cmd typecheck` | PASS |
| `pnpm lint` via `.\pnpm.cmd lint` | PASS, 4 existing warnings |
| `pnpm test` via `.\pnpm.cmd test` | PASS, 357 tests / 49 files |
| `pnpm build` via `.\pnpm.cmd build` | PASS, 4 existing warnings |
| `node scripts/verify-public-access.mjs` | PASS |
| `node scripts/verify-audit-authenticity.mjs` | PASS |
| `node scripts/verify-audit-coverage.mjs` | PASS |
| `node scripts/verify-ops-capture.mjs` | PASS |
| `node scripts/verify-checkout-integrity.mjs` | PASS, HTTP suite skipped because no local app server was configured |
| `node scripts/verify-inventory-integrity.mjs` | PASS |
| `node scripts/verify-checklist-integrity.mjs` | PASS |
| `node scripts/verify-operational-truth.mjs` | PASS |
| `.\pnpm.cmd audit:bundle -- --dry-run` | PASS, secret scan clean, no archive written |

Known lint/build warnings:
- `src/app/actions/auth.test.ts` `_input`
- `src/app/actions/auth.ts` `_prev`, `_formData`
- `src/lib/server/staff-context.test.ts` `_input`

These warnings pre-date V12.5-V12.7 and were present throughout the gated validation runs.

## 3. Migration State

Applied locally:
- `202606071600_v12_5_inventory_stale_count_guard.sql`
- `202606071700_v12_6_checklist_evidence_integrity.sql`

Both versions were registered in `supabase_migrations.schema_migrations` after applying through the running local Supabase Postgres container.

V12.7 did not add a database migration.

## 4. Verification Script Coverage

The V12 verification stack now includes:
- sealed public order access
- audit authenticity and append-only guarantees
- audit coverage for security-event mirroring/redaction
- ops-capture guided workflow and stock-count behavior
- checkout server-side pricing/idempotency/concurrency integrity
- inventory stale-count protection
- checklist evidence integrity
- operational truth states and production demo fallback gating

## 5. Residual Risks / Explicit Non-Certifications

- Checkout HTTP verification remained skipped because no running `CHECKOUT_BASE_URL` was configured. DB/RPC checkout verification passed.
- Some lower-risk loaders outside the owner/storefront V12.7 scope still collapse failures into empty arrays and should be reviewed in a future hardening slice.
- V12.5-V12.7 do not certify production deployment readiness.
- V12.8, V12.9, and final production certification remain pending and untouched.

## 6. Stop Point

All requested V12.5, V12.6, and V12.7 implementation slices are complete, validated, and committed separately. The final combined gate is green.

Stop here. Do not begin V12.8/V12.9/certification.
